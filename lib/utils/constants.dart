import 'package:flutter/material.dart';

const kPrimaryColor = Colors.blueAccent;
const kSecondaryColor = Colors.blue;
const kBackgroundColor = Colors.white;
const kTitleTextColor = Color(0xFF4682B4); // Using a color similar to the provided image
const kCardColor = Colors.white;
const kCardElevation = 4.0;
const kCardMargin = EdgeInsets.symmetric(vertical: 8.0, horizontal: 16.0);
const kCardShape = RoundedRectangleBorder(
  borderRadius: BorderRadius.all(Radius.circular(16.0)),
);
const kButtonColor = Colors.blueAccent;
const kCancelButtonColor = Colors.redAccent;

const kCardColors = [
  Color(0xFFFFE4E1), // Misty Rose
  Color(0xFFFFD700), // Gold
  Color(0xFF98FB98), // Pale Green
  Color(0xFF87CEEB), // Sky Blue
  Color(0xFFD3D3D3), // Light Grey
  Color(0xFF4682B4), // Steel Blue
  Color(0xFFFFA07A), // Light Salmon
];
